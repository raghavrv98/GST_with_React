/*
 * ManageTraderReports Messages
 *
 * This contains all the text for the ManageTraderReports container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ManageTraderReports';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ManageTraderReports container!',
  },
});
