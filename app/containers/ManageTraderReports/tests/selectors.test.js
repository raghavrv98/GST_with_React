// import { fromJS } from 'immutable';
// import { selectManageTraderReportsDomain } from '../selectors';

describe('selectManageTraderReportsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
