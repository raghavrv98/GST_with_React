/*
 * Services Messages
 *
 * This contains all the text for the Services container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Services';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Services container!',
  },
});
