// import { fromJS } from 'immutable';
// import { selectManageAccountantReportsDomain } from '../selectors';

describe('selectManageAccountantReportsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
