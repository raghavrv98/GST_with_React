// import { fromJS } from 'immutable';
// import { selectManagePasswordRequestsDomain } from '../selectors';

describe('selectManagePasswordRequestsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
