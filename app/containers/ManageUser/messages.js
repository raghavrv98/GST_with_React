/*
 * ManageUser Messages
 *
 * This contains all the text for the ManageUser container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ManageUser';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ManageUser container!',
  },
});
