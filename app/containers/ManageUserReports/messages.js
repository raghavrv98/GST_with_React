/*
 * ManageUserReports Messages
 *
 * This contains all the text for the ManageUserReports container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ManageUserReports';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ManageUserReports container!',
  },
});
