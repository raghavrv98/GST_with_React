// import { fromJS } from 'immutable';
// import { selectAccountantNotificationsDomain } from '../selectors';

describe('selectAccountantNotificationsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
