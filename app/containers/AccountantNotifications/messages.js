/*
 * AccountantNotifications Messages
 *
 * This contains all the text for the AccountantNotifications container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AccountantNotifications';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AccountantNotifications container!',
  },
});
