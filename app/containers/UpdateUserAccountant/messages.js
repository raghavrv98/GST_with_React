/*
 * UpdateUserAccountant Messages
 *
 * This contains all the text for the UpdateUserAccountant container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UpdateUserAccountant';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UpdateUserAccountant container!',
  },
});
