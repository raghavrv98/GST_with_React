// import { fromJS } from 'immutable';
// import { selectUpdateUserAccountantDomain } from '../selectors';

describe('selectUpdateUserAccountantDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
