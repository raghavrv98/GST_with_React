/*
 * AdminNotifications Messages
 *
 * This contains all the text for the AdminNotifications container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminNotifications';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminNotifications container!',
  },
});
